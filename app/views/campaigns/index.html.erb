<h1>Campagnes de communication:</h1>

<ul>
  <% @campaigns.each do |campaign| %>
    <li><%= link_to campaign.name, campaign_path(campaign) %></li>
  <% end %>
</ul>


<%= link_to "Créer une campagne",  new_campaign_path %>
